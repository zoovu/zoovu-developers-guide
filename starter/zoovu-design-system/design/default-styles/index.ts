import {default as buttons} from "./buttons";
import {default as checkbox} from "./checkbox";
import {default as radioButton} from "./radio-button";
import {default as textInput} from "./text-input";
const defaultStyles = {buttons, checkbox, radioButton, textInput};
export default defaultStyles;